import 'package:flutter/material.dart';


// Bookly app එකේ සියලුම colors
// එකම තැනක තියාගන්න මේ class එක භාවිතා කරනවා.
class AppColors {

  // Main Olive Green color
  // Bookly app එකේ primary color එක.
  static const Color primary = Color(0xFF6B705C);

  // Light Olive
  // Secondary elements සඳහා භාවිතා කරන්න පුළුවන්.
  static const Color lightOlive = Color(0xFFA5A58D);

  // Warm Cream
  // Main background එක සඳහා.
  static const Color warmCream = Color(0xFFF5F3EC);

  // Off White
  // Cards වගේ UI elements සඳහා.
  static const Color offWhite = Color(0xFFFFFDF8);

  // Dark Charcoal
  // Main text සඳහා.
  static const Color darkText = Color(0xFF2F332B);

  // Sage
  // Light backgrounds / secondary UI සඳහා.
  static const Color sage = Color(0xFFB7B7A4);
}